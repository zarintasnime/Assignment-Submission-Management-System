# Optional. docker-compose.yml already has safe LOCAL DEVELOPMENT defaults.
POSTGRES_DB=assignmentdb
POSTGRES_USER=assignment_user
POSTGRES_PASSWORD=change_this_local_password
JWT_KEY=replace_with_a_long_random_local_key_at_least_32_chars
